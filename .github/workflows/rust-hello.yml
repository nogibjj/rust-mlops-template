name: Rust build and test for `hello` directory

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      working-directory: ./hello
      run: cargo build --verbose
    - name: Run tests
      working-directory: ./hello
      run: cargo test --verbose
